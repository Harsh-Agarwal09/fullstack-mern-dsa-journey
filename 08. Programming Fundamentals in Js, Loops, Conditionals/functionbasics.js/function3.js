function multiple(a, b) {
    // a and b are expected inputs
    console.log(a * b);
}

multiple(5, 10); // 5 & 10 both are actual values for a and b
multiple(3, 7)  ; // 3 & 7 both are actual values for a and b